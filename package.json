{
  "scripts": {
    "tailwind:process": "npx postcss src/style.css --o public/style.css --watch",
    "start": "concurrently \"npm run tailwind:process\" \"npm run serve\"",
    "serve": "eleventy --serve",
    "build": "eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^1.0.1",
    "concurrently": "^7.2.2",
    "daisyui": "^2.15.4",
    "tailwindcss": "^3.1.3"
  },
  "dependencies": {
    "autoprefixer": "^10.4.7",
    "cssnano": "^5.1.11",
    "postcss": "^8.4.14",
    "postcss-cli": "^9.1.0"
  }
}
